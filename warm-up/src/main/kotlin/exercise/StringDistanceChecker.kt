package exercise

class StringDistanceChecker {
    /**
     * This method returns true if two strings have at most distance 1.
     * Strings have distance 0 if they are identical.
     * They have distance 1 if
     *  - one character was replaced,
     *  - one character was added,
     *  - or one character was removed.
     */
    fun isDistanceAtMostOne(string1: String, string2: String): Boolean {
        return false
    }
}
