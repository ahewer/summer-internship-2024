package exercise

class UrlEncoder {

    /**
     * This simple URL encoder is able to perform the following modifications:
     *
     * 1. Trim trailing whitespace
     * 2. Replace whitespace with %20
     *
     */
    fun encode(input: String): String {
        return input
    }
}
