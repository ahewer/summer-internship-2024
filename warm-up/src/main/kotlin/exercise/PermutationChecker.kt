package exercise

class PermutationChecker {

    /**
     * This method will return true if string s1 is a permutation of string s2.
     * Otherwise, it will return false.
     */
    fun isPermutation(s1: String, s2: String): Boolean {
        return false
    }
}
