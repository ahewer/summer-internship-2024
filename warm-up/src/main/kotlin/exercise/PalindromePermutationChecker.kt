package exercise

class PalindromePermutationChecker {
    /**
     * This method returns true if the given string is a permutation of a palindrome.
     * The method ignores letter casing, whitespace, and only consider alphanumeric
     * characters.
     */
    fun checkPermutation(string: String): Boolean {
       return false
    }
}
