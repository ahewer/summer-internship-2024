package exercise

class UniqueCharacters {

    /**
     * This method returns true if the given string only consists of unique characters.
     * If a character occurs more than once, it will return false.
     */
    fun hasOnlyUniqueCharacters(string: String): Boolean {
        return false
    }
}
